---
import { documentToHtmlString } from "@contentful/rich-text-html-renderer";

interface Props {
  content: any;
  class?: string;
}

const { content, class: classList } = Astro.props;
const renderedHTML = documentToHtmlString(content);
---

<div
  set:html={renderedHTML}
  class={`mx-auto mt-16 prose prose-headings:text-brand prose-h2:text-3xl prose-h2:font-smallCaps prose-h2:tracking-wide prose-h2:text-center prose-h2:mt-16 prose-h2:mx-8 prose-h2:mb-8 prose-h3:text-center prose-h3:mt-8 prose-h3:mx-8 prose-h3:mb-4 ${classList || ""}`}
/>
